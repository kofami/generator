name: generator
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.5.0-138.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  args: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:


include:
  - templates/**